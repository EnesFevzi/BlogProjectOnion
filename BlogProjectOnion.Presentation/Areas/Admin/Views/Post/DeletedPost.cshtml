@model List<PostDto>
@{
    ViewData["Title"] = "DeletedPost";
}

<div class="content">
    <div class="container-fluid">
        <h4 class="page-title">Post İşlemleri</h4>
        <div class="row">
            <div class="col-md-12">

                <div class="card">
                    <div class="card-header">
                        <div class="card-title">Silinmiş Post Listesi</div>
                    </div>
                    <div class="card-body">
                        <table class="table mt-3">
                            <thead>
                                <tr>
                                    <th scope="col">Post Adı</th>
                                    <th scope="col">Post Açıklaması</th>
                                    <th scope="col">Sil</th>
                                    <th scope="col">Güncelle</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.Title</td>
                                        <td>@item.Content</td>
                                        <td><a asp-area="Admin" asp-action="UndoDelete" asp-controller="Post" asp-route-postId="@item.PostID" class="btn btn-outline-success">Geri Al</a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

